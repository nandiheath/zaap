apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: metallb
  annotations:
    # apply at last
    argocd.argoproj.io/sync-wave: "5"
spec:
  source:
    path: artifacts/infrastructure/metallb
# to allow metallb to install ClusterRole
#  destination:
#    namespace: metallb-system